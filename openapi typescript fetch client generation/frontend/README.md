#### Steps
- Generate OpenAPI Client
    ```bash
    npm run gen-client
    ```

- Run Build (typescript to javascript)
    ```bash
    npm run build
    ```

- Bundle (javascript to single file)
    ```bash
    npm run bundle
    ```